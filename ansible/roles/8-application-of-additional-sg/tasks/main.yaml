# Refresh CouchDB Instance 0 on MRC with new Security Groups
- name: Create CouchDB instances
  os_server:
    name: '{{ item.name }}'
    image: '{{ item.image }}'
    key_name: '{{ item.key_name }}'
    flavor: '{{ item.flavour }}'
    availability_zone: '{{ item.availability_zone }}'
    nics: 
      - net-name: '{{ item.network }}'
    security_groups: '{{ couchdb0_sg }}'
    volumes: '{{ item.volume }}' 
    auto_floating_ip: yes
    wait: yes
    timeout: 500
    state: present
  loop: '{{ couchdb_instance_0 }}'
  register: os_instance

# Refresh CouchDB Instance 1 on MRC with new Security Groups
- name: Create CouchDB instances
  os_server:
    name: '{{ item.name }}'
    image: '{{ item.image }}'
    key_name: '{{ item.key_name }}'
    flavor: '{{ item.flavour }}'
    availability_zone: '{{ item.availability_zone }}'
    nics: 
      - net-name: '{{ item.network }}'
    security_groups: '{{ couchdb1_sg }}'
    volumes: '{{ item.volume }}' 
    auto_floating_ip: yes
    wait: yes
    timeout: 500
    state: present
  loop: '{{ couchdb_instance_1 }}'
  register: os_instance

# Refresh CouchDB Instance 2 on MRC with new Security Groups
- name: Create CouchDB instances
  os_server:
    name: '{{ item.name }}'
    image: '{{ item.image }}'
    key_name: '{{ item.key_name }}'
    flavor: '{{ item.flavour }}'
    availability_zone: '{{ item.availability_zone }}'
    nics: 
      - net-name: '{{ item.network }}'
    security_groups: '{{ couchdb2_sg }}'
    volumes: '{{ item.volume }}' 
    auto_floating_ip: yes
    wait: yes
    timeout: 500
    state: present
  loop: '{{ couchdb_instance_2 }}'
  register: os_instance

# Refresh Webserver on MRC with new Security Groups
- name: Create Web Front-End Instance
  os_server:
    name: '{{ item.name }}'
    image: '{{ item.image }}'
    key_name: '{{ item.key_name }}'
    flavor: '{{ item.flavour }}'
    availability_zone: '{{ item.availability_zone }}'
    nics: 
      - net-name: '{{ item.network }}'
    security_groups: '{{ webserver_sg }}'
    auto_floating_ip: yes
    wait: yes
    timeout: 500
    state: present
  loop: '{{ web_front_end_instance }}'
  register: os_instance 